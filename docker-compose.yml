services:
  # ── Database ──────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: zenportal-db
    restart: unless-stopped
    command: -p 5433
    environment:
      TZ: ${TIMEZONE:-Asia/Shanghai}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-zenportal}
    ports:
      - "5433:5433"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${DB_USER:-postgres}", "-p", "5433"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Backend ───────────────────────────────────
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
      target: production
      args:
        NPM_REGISTRY: ${NPM_REGISTRY:-}
    container_name: zenportal-backend
    restart: unless-stopped
    expose:
      - "3000"
    environment:
      TZ: ${TIMEZONE:-Asia/Shanghai}
      DATABASE_URL: "postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@postgres:5433/${DB_NAME:-zenportal}"
      PORT: ${PORT:-3000}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-}
      VISITOR_SALT: ${VISITOR_SALT:-}
      MEDIA_MAX_IMAGE_SIZE_MB: ${MEDIA_MAX_IMAGE_SIZE_MB:-10}
      MEDIA_MAX_VIDEO_SIZE_MB: ${MEDIA_MAX_VIDEO_SIZE_MB:-50}
      MEDIA_MAX_AUDIO_SIZE_MB: ${MEDIA_MAX_AUDIO_SIZE_MB:-10}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - media_data:/app/storage
      - backups_data:/app/backups

  # ── Frontend (nginx + static build) ───────────
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      target: production
      args:
        NPM_REGISTRY: ${NPM_REGISTRY:-}
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    container_name: zenportal-frontend
    restart: unless-stopped
    environment:
      TZ: ${TIMEZONE:-Asia/Shanghai}
    depends_on:
      - backend

volumes:
  pgdata:
  media_data:
  backups_data:
